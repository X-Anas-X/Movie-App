import styled from 'styled-components/native';
import {widthDimensionCalculation} from '../../Theme';

export const Title = styled.Text`
  font-size: 30px;
  font-weight: 800;
  margin: ${widthDimensionCalculation(15)}px;
`;
